{
  "name": "@monorepo-template/example",
  "version": "1.0.0",
  "description": "Example application using the shared utils package",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "node dist/index.js",
    "dev": "node --watch dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@monorepo-template/utils": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "*"
  }
}
